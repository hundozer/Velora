// AdminLayout renders the server-authorized command center. This route file is
// deliberately inert so no legacy browser-authority or demo admin UI can mount.
export default function AdminPage() {
  return null;
}
